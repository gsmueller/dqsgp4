# DQSGP4 — Handoff / Next-Session Prompt (2026-06-04)

Branch `session/2026-04-23` in the MAIN repo `C:/Users/graha/source/repos/gps` (NOT a worktree).
Working tree FULLY CLEAN (main + submodule). Submodule `sgp4_references/vallado_celestrak` at gitlink
`447e862`.

## ✅ STATUS: ROADMAP COMPLETE — the Work Queue W0–W24 is fully done

The deterministic roadmap (an internal plan note, now retired) is **finished**. The
sole definition of done — the acceptance harness — confirms it:

```
pwsh tools/run_acceptance.ps1
```
prints **`39/39 gates pass — VERDICT: COMPLETE`** (exit 0). That covers:
- **I0 (standing invariant):** the sln builds; `test_sgp4` = 33/33, 623/623; all 10 module test exes exit 0;
  all 85 Octave `verify_*.m` + 2 Python verifiers pass.
- **W1–W24:** every Work-Queue gate PASS.

`-SkipVerifiers` is the fast inner loop (skips the ~87 Octave/Python verifiers, ~2–3 min); a FULL run
(no flag) exercises everything and is what prints the authoritative `COMPLETE`.

## What this means

The SGP4 / DQSGP4 codebase has reached the roadmap's target state: an authentic-SGP4 implementation that
is **provable per-line** (near-earth Standard-10 + symbolic DQ-algebra + dimensional audits), **fully
traceable** (charter→spec→audit→test, 0 orphans), **quality-gated** (code-consistency, zero magic numbers
in the library, a `ModelValue<T>` provenance type), and **future-proofed** (adaptive RKF7(8) + symplectic
leapfrog integrators, arbitrary-precision validated at `cpp_bin_float_50`, swappable constants). The two
propagators remain independent: `sgp4::Propagator<T>` (analytical, TLE-driven, km/TEME) and
`dynamics::Propagator<T>` (dual-quaternion numerical, m/inertial). See `README.md`,
`design/propagator_choice.md`, `BUILDING.md`.

## There is no next queue item — the next session needs a NEW user objective

The selection rule (lowest-numbered not-Done W-item) has nothing left to select. The next session is
therefore **open**: bring a new goal and it proceeds autonomously from there. A new initiative of any size
should get its own plan (mirroring `recursive-forging-rain.md`'s Work-Queue + objective-DoD structure) so
"done" stays a mechanical verdict.

**Optional follow-ups (NOT mandates — listed only so they aren't forgotten; the user decides if any matter):**
- Branch disposition: all work is on `session/2026-04-23`; the user may want it merged/reviewed (per the
  worktree-management rule, branch lifecycle is the user's call — not presumed here).
- Two BH61-derivation verifiers (`verify_ch10_thm5_hansen_master.m`, `verify_ch11c_critical_inclination.m`)
  print residuals with an `error:` label, which collides with Octave's runtime `error:`; the harness now
  classifies correctly regardless, but rewording those labels would be tidy.
- Extend the magic-number policy (currently scoped to the 5 reusable-library dirs per AUD-CC-15) to the
  SGP4 application code — but those are authentic-algorithm reference constants governed by the 33/33
  fidelity gate, so this needs a deliberate provenance pass, not a blind sweep.

## Conventions (binding for any reopened work)

- **Completion = `tools/run_acceptance.ps1` prints `COMPLETE`.** Its verdict line IS the project status.
- Regression-gate **test_sgp4 33/33, 623/623** on every code-touching commit; one session = one branch
  (`session/2026-04-23`); commit per logical item; no worktrees.
- **BUILD:** full-path MSBuild —
  `"C:\Program Files\Microsoft Visual Studio\18\Enterprise\MSBuild\Current\Bin\MSBuild.exe" "<repo>\sgp4.sln" /p:Configuration=Release /p:Platform=x64 /m`
  (invoke directly via PowerShell `&`). Standalone `.cpp` → `vcvars64.bat` then
  `cl /std:c++20 /EHsc /I vcpkg_installed\x64-windows\include /I src /c <file>`.
- **New test exe:** mirror `tests/test_force_models/` — a `main.cpp` + a `.vcxproj` copied from an existing
  one with a FRESH GUID, then register it in `sgp4.sln` (the `Project(...)` line + its 4 Debug/Release
  config entries). The harness gates pick it up via the `ExeGate`/`VerifierGate`/`ScriptGate` already wired
  in `tools/run_acceptance.ps1`.
- **`src/` headers** must stay AUD-CC clean (namespace, `#pragma once`, ≤100 cols, `///` docs, no
  `using namespace`) and magic-number clean (constants via `exact<T>(n)` / `ratio<T>(p,q)` / `T("…")`, or a
  justified `// magic-ok:` for an irreducible literal). `tools/check_code_consistency` / `check_magic_numbers`
  scan the 5 library dirs (`math, dynamics, constants, forces, integrators`).
- **Octave verifier idiom:** start with `1;`, `pkg load symbolic`, exact rationals via `sym(p)/q`, no
  script-local functions (use anonymous fns); `exit(0)`/`exit(1)`; keep `[FAIL]`/`error:` out of passing
  output (the harness keys failure on `!!!`, `[FAIL]`, `FAILED`, or a nonzero `N FAIL` count).
  Octave at `C:\Program Files\GNU Octave\Octave-11.1.0\mingw64\bin\octave-cli.exe`.

## Where the history lives (this completion was reached over many sessions)

- `memory/session_2026_06_04_roadmap_p2.md` — ★ the live state: the whole W0–W24 arc, the per-item detail,
  the harness-fix discovery, the magic-number scope finding, and every gotcha. Read this first.
- `memory/session_2026_06_03c_sdp4_complete.md` — deep-space (SDP4) completion (33/33).
- `memory/session_2026_06_03d_auto_removal.md` — the `auto`-removal pass.
- `memory/session_2026_06_03_xmcof_C3_clean.md` — the SGP4 drag derivation.
- `design/derivations/sgp4_drag_phase_plan.md` — the durable drag-derivation plan.
- `design/index.md` — the charter→spec→audit→test governance index (kept honest by `check_index.ps1`).

> Earlier revisions of this handoff (the W8-next banner, the deep-space/drag investigation logs, the
> auto-removal and build-recipe notes) are superseded; that detail is preserved in the memory files above.
